鱼皮模拟面试



Q：

1. 自我介绍 不够自信不够流畅

2. HashMap
   1. 有没有遇到过hashmap并发冲突 什么情况下会遇到并发修改冲突
   2. HashMap和HashSet有什么区别 HashSet是怎么去实现的Hash的算法
   3. HashMap的负载因子 什么时候会用到 如何用？内存资源紧张的情况下如何设置负载因子？
   4. 遍历HashMap的方式
   5. 有了解过StreamAPI 如何对一个集合的元素 按照.0某个字段分组？
   6. StreamAPI是如何实现的？是这个StreamAPI如何实现的 还是它如何实现HashMap的遍历呢？
   7. 是怎么把集合转成流式的？
   8. StreamAPI把集合列表数组转化成一个标准的对象？如何实现的链式调用？如何... 然后最后一个collect把数据收集到一起去？
   9. HashMap中如何要存储大量的数据，有哪些优化HashMap的性能？
   
      
   
      1. | 调整初始容量       | 数据量可预估                  | 内存占用增加          |
         | ------------------ | ----------------------------- | --------------------- |
         | 降低负载因子       | 对查询性能要求极高            | 内存占用显著增加      |
         | 分片处理           | 超大数据量（千万级+）         | 代码复杂度提高        |
         | 使用Trove/Koloboke | 键为原始类型（int/long）      | 失去泛型特性，API差异 |
         | 树化优化           | 存在长链表（Java 8+默认支持） | 轻微增加插入时间      |
   
3. 工作经验
   1. 在你的工作过程中 比较有挑战的工作 或者某个难度比较大的功能/印象比较深的功能
   2. 如何实现异步编程？59分 答对了 但是答得不好
      1. 使用默认线程池的缺陷
         - **并行度不足**
         - 所有 CompletableFuture 任务共用同一个公共池（包括其他业务逻辑）
         - **阻塞操作不兼容** HTTP 请求是阻塞 I/O 操作
         - 任务堆积无界	默认使用无界队列（LinkedBlockingQueue 变体） 造成内存溢出
         - 高延迟
   3. 异步使用线程池的话 如何实现的线程池 或者说**使用线程池的时候有哪些技巧**呢？
      1. 自定义线程池
         - 50核心数
         - 最大线程数量200
         - 线程存活时间30s
         - 有界队列1000上限
         - 自定义线程名称
         - 拒绝策略（线程满后由调用线程执行） 
   4. 使用线程池有哪些使用技巧和哪些注意事项呢？（实质上问的问题 线程池的每个核心参数应该如何设计）
   
4. NGINX
   1. 如何使用NGINX同时部署多个网站
   2.  如何把域名映射到不同的NGINX的配置呢？Server块  vhost 虚拟主机配置
   
5. 为什么要用双检索单例模式

6. MySQL 
   1. MySQL的字段类型有哪些 日期的类型 date datetime timestamp
   2. datetime 和 timestamp 的区别
   3. 字符串存日期会有什么问题
   
7. 如何设计一个复杂的审批系统 请假功能的

  



鱼皮哥建议：

1. 大部分问题都能回答出来 但是回答的不是特别好（泛泛水平 不精通 浅尝辄止） 
2.  继续背八股
3. 结合自身工作中的项目经历 去准备一些八股文 把对应的八股文再运用到项目中 比如 **使用线程池的技巧** 实践于项目中 以及 datetime 和 timestamp 的区别 （既是八股文 又是工作经验）timestamp是可以基于时区去做转换的 比如 timestamp有一个限制 只能存到2038年的数据
4. 扩展下AI相关的知识面 看一下鱼皮哥最新发布的视频 AI相关的







3.18面试

工作项目如何部署的？（）单服务器部署

如果改成分布式 有哪些需要改动的或者注意的事项吗？

什么情况或者情景下使用HashMap？

HashMap的作用 高效查找

字符串拆分和组合如何自己实现

如何解决跨域问题的

有用过容器化部署吗

